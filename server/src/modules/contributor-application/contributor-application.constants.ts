/**
 * Reputation score at which a student is shown as "eligible". A hint only —
 * eligibility never gates applying; the admin makes the decision (decided
 * 2026-09-20, BACKLOG.md E6). 10 = five unique post upvotes (see E5 prices).
 */
export const CONTRIBUTOR_ELIGIBILITY_SCORE = 10;

export const APPLICATION_REASON_MIN = 30;
export const APPLICATION_REASON_MAX = 1000;
